import preloader from '..//avatarka/loading-thinking.gif'

let Preloader = (props) => {
  return (
    <div style={{ backgroundColor: 'white' }}>
      <img src={preloader} />
    </div>
  )
}

export default Preloader
